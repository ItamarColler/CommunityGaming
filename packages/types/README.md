# @community-gaming/types

Shared TypeScript types and Zod validation schemas.

## Usage

```typescript
import { UserSchema, type User } from '@community-gaming/types';

const user = UserSchema.parse(data);
```
